#!/bin/sh

FILE_TAGS="amsua_n17 hirs3_n17 ssmis_f16"

for TAG in ${FILE_TAGS}; do
  INPUT_FILE="${TAG}.input"
  RESULTS_FILE="${TAG}.results"
  if [ -f ${INPUT_FILE} ]; then
    Forward_Model_Example < ${INPUT_FILE} > ${RESULTS_FILE}
  fi
done
